# CloneUp — local / spike dependencies
# Install:  .venv\Scripts\python.exe -m pip install -r requirements.txt

python-dotenv==1.2.2
requests==2.34.2
keyring==25.7.0

# Later (UI): PySide6
# Later (packaging): pyinstaller
