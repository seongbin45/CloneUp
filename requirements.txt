# CloneUp — local / spike dependencies
# Install:  .venv\Scripts\python.exe -m pip install -r requirements.txt

python-dotenv==1.2.2
requests==2.34.2
keyring==25.7.0
cryptography>=42.0.0
PySide6>=6.6.0
# Qt WebEngine (Chromium) — in-app GitHub connect wizard. Same version as PySide6.
PySide6-Addons>=6.6.0

# Optional: read Chrome/Edge address bar for external Google handoff guide
#   pip install uiautomation
uiautomation>=2.0.18

# Path B Expiration: screenshot + dual OCR (Windows.Media.Ocr + Tesseract)
# Tesseract binary: bundled in CloneUp-Setup (task) or winget / TESSERACT_CMD
Pillow>=10.0.0
pytesseract>=0.3.10
winrt-runtime>=3.0
winrt-Windows.Foundation>=3.0
winrt-Windows.Foundation.Collections>=3.0
winrt-Windows.Globalization>=3.0
winrt-Windows.Graphics.Imaging>=3.0
winrt-Windows.Media.Ocr>=3.0
winrt-Windows.Storage.Streams>=3.0

# Optional experimental Device Flow helper (CLONEUP_PLAYWRIGHT=1):
#   pip install playwright && playwright install chromium
# playwright>=1.40.0

# Packaging (optional):
#   pip install pyinstaller
#   powershell -File scripts\build_exe.ps1
# pyinstaller>=6.0
