# CloneUp — local / spike dependencies
# Install:  .venv\Scripts\python.exe -m pip install -r requirements.txt

python-dotenv==1.2.2
requests==2.34.2
keyring==25.7.0
cryptography>=42.0.0
PySide6>=6.6.0
# Qt WebEngine (Chromium) — in-app GitHub connect wizard. Same version as PySide6.
PySide6-Addons>=6.6.0

# Optional: read Chrome/Edge address bar for external Google handoff guide
#   pip install uiautomation
uiautomation>=2.0.18

# Path B Expiration: screenshot + OCR (visible "30 days" on classic PAT form)
# Also needs Tesseract binary: winget install UB-Mannheim.TesseractOCR
# Or set TESSERACT_CMD to tesseract.exe
Pillow>=10.0.0
pytesseract>=0.3.10

# Optional experimental Device Flow helper (CLONEUP_PLAYWRIGHT=1):
#   pip install playwright && playwright install chromium
# playwright>=1.40.0

# Packaging (optional):
#   pip install pyinstaller
#   powershell -File scripts\build_exe.ps1
# pyinstaller>=6.0
