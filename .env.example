# Optional local overrides only.
# Packaged builds use defaults in app/config.py when these are unset.
# client_id is not a secret (public OAuth client).

# GITHUB_CLIENT_ID=
# Default app scope is `repo` (public + private create/push).
# GITHUB_SCOPES=repo
