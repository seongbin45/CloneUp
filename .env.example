# Optional local overrides only.
# Packaged builds use the built-in DEFAULT_GITHUB_CLIENT_ID in app/config.py
# when these are unset. client_id is not a secret (public OAuth client).

# GITHUB_CLIENT_ID=
# GITHUB_SCOPES=public_repo
